# dsh-plugin-everything-search bundle patch: inserts the host tool plugin row
# into the profile roster. Install with:
#   dsh plugin --profile web add github:SaigyoujiCTakuhei/dsh-plugin-everything-search
- insert:
    - id: everything-search
      name: 'dsh-plugin-everything-search'
