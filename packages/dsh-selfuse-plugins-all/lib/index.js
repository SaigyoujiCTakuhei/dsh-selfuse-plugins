/** dsh-selfuse-plugins-all — aggregate host half.
 * This package ships no feature of its own; it only mounts the sibling plugins
 * through cordis.patch.yml. Single-plugin installs remain available via each
 * subpackage's own cordis.patch.yml.
 */
const inject = [];
function apply(_ctx) {}
export { apply, inject };
